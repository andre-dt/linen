{
  "name": "@linen/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=5174 tsx watch main.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "build:native": "cd occt && conan install . --output-folder=build --build=never && node-gyp rebuild"
  },
  "dependencies": {
    "@linen/cad": "workspace:*",
    "@linen/protocol": "workspace:*",
    "ws": "catalog:",
    "isomorphic-git": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "@types/ws": "catalog:",
    "tsx": "^4.19.2"
  }
}
