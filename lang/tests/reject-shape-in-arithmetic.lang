#~ works on numbers
# A shape is not a number. There is no elementwise arithmetic and no
# implicit conversion to one of its fields.

shape Point
  x i32
  y i32

test "t"
  let p = Point(x: 1, y: 2)
  throw "m" unless p + p == 4
