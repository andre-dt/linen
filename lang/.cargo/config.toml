[env]
# Where llvm-sys finds LLVM 18. Pinned in the repo rather than left to a
# shell variable: a build that works in one terminal and fails in another
# is a build nobody can reason about, and this is the one dependency that
# cannot be vendored.
LLVM_SYS_181_PREFIX = "/usr/lib/llvm-18"
